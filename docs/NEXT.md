# Next steps (session handoff)

**Update this file when closing a session** so the next run knows where to continue.

- **Next:** Merge PR #45 (TECH-1001) if not already merged. Then run the bulk prompt in [BULK-PROMPT-5-SMALL-TASKS.md](./BULK-PROMPT-5-SMALL-TASKS.md) for tasks 0042 → 0045 → 0047 → supply list (TECH-0030) → 0044. Test after each merge (see AGENTS.md “Testing bulk-task PRs one at a time”).
