#!/bin/sh
# Same checks as `pnpm run prepush`; avoid `pnpm` here (Git Bash + Windows Corepack shim breaks).
echo "husky(pre-push): eslint..."
eslint
echo "husky(pre-push): tsc --noEmit (prepush config, can take a minute)..."
tsc --noEmit -p tsconfig.prepush.json
echo "husky(pre-push): ok"
