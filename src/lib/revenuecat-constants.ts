/**
 * Shared RevenueCat identifiers (safe for client and server).
 * Keep these in sync with the RevenueCat dashboard product catalog.
 */

/** Entitlement that unlocks all Pro features. */
export const PRO_ENTITLEMENT_ID = "anthony & omolola enterprises inc. Pro";

/** Product identifiers configured in the RevenueCat dashboard. */
export const REVENUECAT_PRODUCT_IDS = {
  lifetime: "lifetime",
  yearly: "yearly",
  monthly: "monthly",
} as const;
